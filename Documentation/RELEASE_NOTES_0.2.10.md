# Even More Gestures 0.2.10

- The Apps screen now summarizes the preset and custom apps currently shown instead of exposing the internal preset-library count.
- Filter your list by all apps, presets, custom apps, or apps with a saved override.
