# Even More Gestures 0.2.26

- Fixed an issue where a defaults cache from an earlier app version could hide newer bundled shortcuts, including Chrome’s `⌃G` right-sidebar action.
- New app versions now show their bundled defaults immediately, then continue to refresh signed defaults daily.
